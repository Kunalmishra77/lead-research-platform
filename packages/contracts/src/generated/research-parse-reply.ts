/* DO NOT EDIT. Generated by packages/contracts/scripts/gen.ts from schemas/. */

/**
 * What a worker sends back for a research.parse request (ADR-0005). The API waits for exactly one of these on the reply key it generated. A failure is a reply too, so the caller fails immediately instead of waiting out the timeout.
 */
export type ResearchParseReply = {
  reply_version: 1;
  job_id: string;
  ok: boolean;
  result?: ParseResult;
  error?: ParseError;
};

/**
 * This interface was referenced by `undefined`'s JSON-Schema
 * via the `definition` "ParseResult".
 */
export interface ParseResult {
  /**
   * A ResearchSpec, already validated against research-spec.schema.json by the worker.
   */
  spec: {};
  /**
   * @maxItems 40
   */
  feasibility: ParseFeasibility[];
  /**
   * True when the user should be asked before credits are spent: something was ambiguous, unsupported, or there is nowhere to search.
   */
  needs_confirmation: boolean;
  /**
   * @maxItems 5
   */
  ambiguities?: string[];
  /**
   * Constraints the user stated that the spec cannot express, so the UI can say so rather than quietly ignoring them.
   *
   * @maxItems 8
   */
  unsupported?: string[];
  confidence: number;
  provenance: ParseProvenance;
}
/**
 * Whether a filter can actually be searched on, or only applied afterwards (docs/08 capability map).
 *
 * This interface was referenced by `undefined`'s JSON-Schema
 * via the `definition` "Feasibility".
 */
export interface ParseFeasibility {
  filter: string;
  mode: "directly_searchable" | "post_filter" | "estimate_only" | "unsupported";
  note?: string;
}
/**
 * Which model and prompt produced this reading (docs/07: every AI-derived value records both).
 *
 * This interface was referenced by `undefined`'s JSON-Schema
 * via the `definition` "ParseProvenance".
 */
export interface ParseProvenance {
  model: string;
  prompt_version: string;
  observed_at: string;
  /**
   * True when the answer came from the gateway's response cache rather than a fresh model call.
   */
  cached?: boolean;
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema
 * via the `definition` "ParseError".
 */
export interface ParseError {
  error_class:
    "transient" | "rate_limited" | "access_restricted" | "parse_failed" | "invalid_input" | "budget_exhausted";
  message: string;
}
