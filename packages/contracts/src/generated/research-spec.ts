/* DO NOT EDIT. Generated by packages/contracts/scripts/gen.ts from schemas/. */

/**
 * Output field catalogue (one key per social platform; X = formerly Twitter). Which connectors can fill each field lives in the planner capability map (docs/08). Adding a key is additive for producers but deploy consumers (workers) first: they reject unknown keys.
 *
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "FieldKey".
 */
export type FieldKey =
  | "name"
  | "category"
  | "industry"
  | "description"
  | "website"
  | "phone"
  | "email"
  | "whatsapp"
  | "contact_form"
  | "address"
  | "city"
  | "state"
  | "country"
  | "postal_code"
  | "geo"
  | "google_maps_url"
  | "rating"
  | "review_count"
  | "business_status"
  | "opening_hours"
  | "instagram"
  | "facebook"
  | "linkedin"
  | "x"
  | "youtube"
  | "employee_band"
  | "founded_year"
  | "technologies"
  | "hiring"
  | "people";

/**
 * Typed research request (docs/06 section 1). v1: finalized in Phase 2 task 2.1. Additive changes only within v1.
 */
export interface ResearchSpec {
  spec_version: 1;
  entity: "company";
  intent: "prospecting" | "market_map" | "competitor_scan" | "hiring_signal" | "single_company";
  seed_company?: SeedCompany;
  filters: Filters;
  keywords?: Keywords;
  /**
   * Output fields. uniqueItems is enforced by the TS validator; specs are built and validated in the API.
   *
   * @minItems 1
   * @maxItems 30
   */
  fields: FieldKey[];
  /**
   * @maxItems 10
   */
  custom_columns?: CustomColumn[];
  depth: "quick" | "standard" | "deep";
  limits: Limits;
  exclude?: Exclude;
  feasibility?: Feasibility[];
}
/**
 * The company a request is about: the one to profile for a single_company run, or the one to find competitors of. Without it a competitor_scan has nothing to compare against.
 *
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "SeedCompany".
 */
export interface SeedCompany {
  name: string;
  website?: string;
}
/**
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "Filters".
 */
export interface Filters {
  industry?: {
    /**
     * @maxItems 20
     */
    include?: string[];
    /**
     * @maxItems 20
     */
    exclude?: string[];
    /**
     * Industry taxonomy slugs (app.industries.slug).
     *
     * @maxItems 20
     */
    taxonomy_ids?: string[];
  };
  location?: Location;
  employee_count?: IntRange;
  has_website?: boolean | null;
  contact?: {
    any_of?: ("email" | "phone" | "whatsapp" | "contact_form")[];
  };
  social?: {
    [k: string]: SocialFilter;
  };
  technologies?: IncludeExclude;
  hiring?: {
    required?: boolean;
    departments?: string[];
  };
  funding?: {
    last_round_within_days?: number | null;
  };
  rating?: {
    gte?: number | null;
    min_reviews?: number | null;
  };
  min_confidence?: number;
}
/**
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "Location".
 */
export interface Location {
  /**
   * ISO-3166 alpha-2
   */
  country?: string;
  /**
   * @maxItems 40
   */
  states?: string[];
  /**
   * @maxItems 50
   */
  cities?: string[];
  radius_km?: number | null;
  include_metro_area?: boolean;
}
/**
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "IntRange".
 */
export interface IntRange {
  gte?: number | null;
  lte?: number | null;
  allow_estimate?: boolean;
}
/**
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "SocialFilter".
 */
export interface SocialFilter {
  required?: boolean;
  active_within_days?: number | null;
}
/**
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "IncludeExclude".
 */
export interface IncludeExclude {
  /**
   * @maxItems 50
   */
  include?: string[];
  /**
   * @maxItems 50
   */
  exclude?: string[];
}
/**
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "Keywords".
 */
export interface Keywords {
  /**
   * @maxItems 20
   */
  must?: string[];
  /**
   * @maxItems 20
   */
  should?: string[];
  /**
   * @maxItems 20
   */
  not?: string[];
}
/**
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "CustomColumn".
 */
export interface CustomColumn {
  name: string;
  prompt: string;
  output_type: "boolean" | "text" | "number" | "enum";
}
/**
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "Limits".
 */
export interface Limits {
  max_results: number;
  max_credits: number;
}
/**
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "Exclude".
 */
export interface Exclude {
  lists?: string[];
  existing_leads?: boolean;
}
/**
 * This interface was referenced by `ResearchSpec`'s JSON-Schema
 * via the `definition` "Feasibility".
 */
export interface Feasibility {
  filter: string;
  mode: "directly_searchable" | "post_filter" | "estimate_only";
  note?: string;
}
