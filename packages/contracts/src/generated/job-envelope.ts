/* DO NOT EDIT. Generated by packages/contracts/scripts/gen.ts from schemas/. */

/**
 * Envelope for every job published on a Redis Stream (docs/01, ADR-0001). Version 1.
 */
export interface JobEnvelope {
  /**
   * Bumped on breaking envelope changes.
   */
  envelope_version: 1;
  /**
   * UUID v7 of this job.
   */
  job_id: string;
  /**
   * <domain>.<action>, e.g. crawl.fetch, system.ping.
   */
  type: string;
  /**
   * Tenant; null only for global (non-tenant) work.
   */
  org_id: string | null;
  research_job_id: string | null;
  idempotency_key: string;
  /**
   * 1-based delivery attempt; max 5 matches the retry policy (DLQ entries keep attempt 5).
   */
  attempt: number;
  priority: "interactive" | "scheduled" | "backfill";
  budget: Budget;
  trace_id: string;
  /**
   * Job-type specific payload, validated by the handler.
   */
  payload: {
    [k: string]: unknown;
  };
  created_at: string;
}
/**
 * This interface was referenced by `JobEnvelope`'s JSON-Schema
 * via the `definition` "Budget".
 */
export interface Budget {
  credits_remaining: number;
  /**
   * Internal cost cap in USD micros (integer money rule).
   */
  cost_cap_micros: number;
}
